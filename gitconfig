[user]
  name = Karl Yngve Lervåg
  email = karl.yngve+git@gmail.com

[core]
  pager = less
  editor = vim
  excludesfile = ~/.dotfiles/gitignore

[color]
  ui = auto
  branch = auto
  diff = auto
  interactive = auto
  status = auto

[alias]
  ci = commit
  co = checkout
  st = status
  lg = log --all --decorate --graph --date=short --format=format:'%Cblue%h%Creset %Cgreen%ad (%ar)%C(bold yellow)%d%Creset%n        %C(white)%s%Creset%n        %an'

[diff]
  algorithm = patience

[merge]
  tool = mydiff
  conflictStyle = diff3

[mergetool]
  keepBackup = false
  prompt = false

[mergetool "mydiff"]
  cmd = xvim -f -d $LOCAL $MERGED $REMOTE -c "MergeMode"

[push]
  default = simple

[grep]
  extendedRegexp = true
