[user]
  name = Karl Yngve Lervåg
  email = karl.yngve+git@gmail.com

[core]
  pager = less
  editor = vim
  excludesfile = ~/.dotfiles/gitignore

[color]
  ui = auto
  branch = auto
  diff = auto
  interactive = auto
  status = auto

[alias]
  logr = log --reverse --name-status
  pull = pull --ff-only
  merge = merge --no-ff
  up = merge --ff-only
  ci = commit
  co = checkout
  stat = status
  st = status
  g = grep --break --heading --line-number

[merge]
  tool = mytool

[mergetool "splice"]
  cmd = "gvim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
  trustExitCode = true

[mergetool "mytool"]
  cmd = vim +GitMerge -d $LOCAL $MERGED $REMOTE

[push]
  default = simple

[grep]
  extendedRegexp = true
